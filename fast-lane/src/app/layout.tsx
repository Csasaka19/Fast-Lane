import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/providers/ThemeProvider";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Fastlane | Turn Strategy Into Scalable Growth",
  description:
    "We help ambitious businesses stop guessing and start scaling. We engineer high-ROI offers, build conversion systems, and turn attention into predictable revenue.",
  keywords: [
    "business growth",
    "ROI optimization",
    "offer structuring",
    "lead generation",
    "business consulting",
    "Nairobi",
    "Kenya",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        <ThemeProvider>
          <Navbar />
          <main className="min-h-screen">{children}</main>
          <Footer />
        </ThemeProvider>
      </body>
    </html>
  );
}
